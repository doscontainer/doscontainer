[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { workspace = true }
hwspec = { path = "../hwspec" }
manifest = { path = "../manifest" }
planner = { path = "../planner" }